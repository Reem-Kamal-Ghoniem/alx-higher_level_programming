#!/usr/bin/python3
def islower(c):
    """function that decides id the letter
    is uppercase or lower case"""
    if (ord(c) >= 97 and ord(c) <= 122:
        return True
    else:
        return False
